# =============================================================================
# requirements.txt
# Enhancing Automated ICD-10 Medical Coding with Large Language Models
# Master's Thesis - California State University, Sacramento
# Author: Namirah Imtieaz Shaik
# =============================================================================
#
# Install all dependencies with:
#   pip install -r requirements.txt
#
# GPU is strongly recommended for all LLM training and inference.
# All experiments were run on NVIDIA A100 / L40 GPUs.
#
# Python version: 3.10 or higher recommended
# CUDA version:   11.8 or higher recommended
# =============================================================================

# ---- Core Deep Learning ----
torch>=2.1.0
torchvision>=0.16.0

# ---- HuggingFace Ecosystem ----
transformers>=4.40.0
datasets>=2.18.0
tokenizers>=0.19.0
accelerate>=0.29.0
huggingface_hub>=0.22.0
safetensors>=0.4.2

# ---- PEFT / LoRA / QLoRA ----
peft>=0.10.0
bitsandbytes>=0.43.0

# ---- TRL (SFT Trainer for Generative Models) ----
trl>=0.8.6

# ---- Classical ML Baseline ----
scikit-learn>=1.4.0
scipy>=1.12.0

# ---- Data Handling ----
numpy>=1.26.0
pandas>=2.2.0

# ---- Hyperparameter Tuning ----
ray[tune]>=2.10.0

# ---- Evaluation Metrics ----
# sklearn already covers F1, ROC-AUC, classification report
# No additional metric libraries needed

# ---- Visualization ----
matplotlib>=3.8.0

# ---- SHAP Explainability (Demo) ----
shap>=0.45.0

# ---- Gradio Demo ----
gradio>=4.26.0

# ---- Utilities ----
tqdm>=4.66.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# =============================================================================
# NOTES ON SPECIFIC PACKAGES
# =============================================================================
#
# bitsandbytes:
#   Required for QLoRA 4-bit NF4 quantization (OpenBioLLM-8B training and eval).
#   Also used for AdamW8bit optimizer in HPT scripts.
#   Windows users may need: pip install bitsandbytes --prefer-binary
#
# ray[tune]:
#   Required only for hyperparameter tuning scripts (ray_tune_exp.py and
#   ray_tune_biomistral.py). Can be skipped if not running HPT.
#
# shap:
#   Required only for the Gradio demo (Demo/app.py).
#   Can be skipped if not running the explainability UI.
#
# gradio:
#   Required only for the Gradio demo (Demo/app.py).
#   Can be skipped if not running the UI.
#
# sentencepiece + protobuf:
#   Required by the Meditron and BioMistral tokenizers.
#   Without these the tokenizer will fail to load.
#
# torch version:
#   Install the CUDA-enabled version for GPU support. Visit pytorch.org
#   for the correct install command for your CUDA version. Example:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
# =============================================================================
